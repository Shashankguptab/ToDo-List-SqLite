# To-Do List (Python + SQLite)

A simple command-line To-Do List application built using Python and SQLite.  
This project demonstrates basic CRUD operations using a database.

## Features
- Add a task
- View all tasks
- Mark task as completed
- Delete a task

## Tech Stack
- Python
- SQLite (built-in database)
- SQL (CRUD operations)
